services:
  - type: web
    name: fotocopiadora
    runtime: python
    buildCommand: pip install fastapi uvicorn jinja2 python-multipart aiofiles pysnmp
    startCommand: uvicorn server.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
